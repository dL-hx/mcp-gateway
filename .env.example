# 通义千问（阿里云 DashScope）API Key，用于 AI 市场对话
# 获取方式：阿里云控制台 -> DashScope -> API-KEY 创建
# 注意：本文件已在 .gitignore 中被忽略，不会被 git 提交

# 默认模型
MYTRADE_DEFAULT_MODEL=qwen-turbo

# DashScope API 密钥
DASHSCOPE_API_KEY=sk-xxxxxxxxxx


